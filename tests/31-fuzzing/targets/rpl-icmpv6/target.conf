# Fuzz the RPL control message parsers by injecting at the ICMPv6 layer,
# which avoids having to satisfy the IPv6 header checks on every input.
FUZZ_ENTRY_POINT=icmpv6
FUZZ_PROTOCOLS=""
FUZZ_DICTIONARY=rpl.dict
